{
  "name": "@head.js/panc",
  "description": "判词 Panc",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "lerna run build --stream",
    "build:core": "lerna run build --scope @head.js/panc-core",
    "build:rum-core": "lerna run build --scope @head.js/panc-rum-core",
    "build:rum-slim": "lerna run build --scope @head.js/panc-rum-slim",
    "build:bundle": "lerna run build:bundle --stream"
  },
  "devDependencies": {
    "@types/jasmine": "3.10.18",
    "jasmine-core": "3.99.1",
    "lerna": "8.1.9",
    "npm-run-all": "4.1.5"
  }
}
